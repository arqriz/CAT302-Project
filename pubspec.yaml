name: regen
description: Gamified Recycling System for USM
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  provider: ^6.1.1 
  intl: ^0.19.0
  url_launcher: ^6.2.2
  percent_indicator: ^4.2.3

  # UPDATED FIREBASE STACK (Recommended for 2025)
  firebase_core: ^3.10.0
  firebase_auth: ^5.4.0
  cloud_firestore: ^5.6.0
  google_sign_in: ^6.2.1  # Most stable for standard Flutter auth

  image_picker: ^1.0.4


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  assets:
  - assets/images/

